t = 0 
tax = 0 

def total(q, p):
  global t 
  global tax
  t = (float(p) * float(q))
  tax = t * 0.07
  

  return t, tax

q = float(input("Enter quantity: "))
p = float(input("Enter the unit price of the item: "))


total(q, p) 


print("Total is: ",  t)
print("Tax is: ", tax)